# Wordle Helper (RU & EN)

Excel-based Wordle assistant + Python scripts for dictionary filtering.

## Note
Raw dictionary files are not included in the repo due to size. Place them locally and run the scripts to generate filtered lists.
